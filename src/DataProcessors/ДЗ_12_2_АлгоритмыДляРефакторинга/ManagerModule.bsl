
Функция ТяжелаяОперация() Экспорт
	
	ГСЧ = Новый ГенераторСлучайныхЧисел;
	
	КонецВыполнения = ТекущаяДатаСеанса() + 10;
	
	Пока ТекущаяДатаСеанса() < КонецВыполнения Цикл
		// Очень плохая реализация паузы
		// Никогда не делайте так в реальных проектах
		// Полностью занимает 1 ядро процессора на сервере
	КонецЦикла;
	
	Возврат ГСЧ.СлучайноеЧисло(1, 100);
	
КонецФункции